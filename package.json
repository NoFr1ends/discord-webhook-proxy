{
  "name": "discord-webhook-proxy",
  "version": "0.5.0",
  "description": "A webhook which transform incoming data to discord webhook",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonas Bernemann",
  "repository": {
    "type": "git",
    "url": "https://github.com/DevChuckNorris/discord-webhook-proxy"
  },
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "discord.js": "^11.5.1",
    "express": "^4.17.1",
    "morgan": "^1.9.1",
    "request": "^2.88.0"
  },
  "bin": {
    "discord-webhook-proxy": "./bin/discord-webhook-proxy"
  }
}
